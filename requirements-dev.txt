# Dependencias adicionales SOLO para desarrollo y pruebas — no se instalan
# en la imagen Docker de producción (ver Dockerfile: usa requirements.txt
# solo, nunca este archivo).
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
pytest-cov==4.1.0
